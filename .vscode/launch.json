{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Attach to Node Process",
            "port": 9229,
            "localRoot": "${workspaceFolder}/app/code",
            "remoteRoot": "${workspaceFolder}/app/code",
            "skipFiles": [
                "<node_internals>/**"
            ]
        }
    ]
}